spring:
  application:
    name: car-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest